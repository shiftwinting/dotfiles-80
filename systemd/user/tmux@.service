[Unit]
Description=Start tmux in detached session
Documentation=man:tmux(1)

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/usr/bin/tmux -f %h/.config/tmux/tmux.conf new-session -Ad -s %I
ExecStop=/usr/bin/tmux kill-session -t %I

[Install]
WantedBy=default.target
