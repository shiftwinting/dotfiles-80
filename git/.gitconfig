[user]
	email = rui.ferreira@xesolinnovation.com
	name = Rui Marques
        username = rui.ferreira
[push]
	default = simple
[core]
	editor = nvim
	whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
	excludesfile = ~/gitignore
[web]
	browser = firefox
[diff]
        tool = nvimdiff2
[difftool "nvimdiff2"]
        cmd = nvim -d $LOCAL $REMOTE
        trustExitCode = true
[merge]
        tool = nfugitive
            # if not using a tool name with builtin support, must supply mergetool cmd
                # as below
[mergetool "nvimdiff4"]
        cmd = nvim -d $LOCAL $BASE $REMOTE $MERGED -c '$wincmd w' -c 'wincmd J'
[mergetool "nfugitive"]
        cmd = nvim -f -c "Gdiff" "$MERGED"
[commit]
	verbose = true
