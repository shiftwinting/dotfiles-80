#!/usr/bin/env sh
set -e

i3-msg workspace 10
if [ "$(hostname)" = "dev-004" ]; then
    dbus-launch --autolaunch="$(cat /var/lib/dbus/machine-id)" spotify &
else
    spotify &
fi
i3-msg split v
$TERMINAL -e cava &

sleep 3

i3-msg move up
